name: Python test

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Linting with Python
      uses: vcoder4c/git_pylint@v3
      with:
        args: /
        format: 'json'
        fail-under: 8
        output: pylint-result.xml
      
